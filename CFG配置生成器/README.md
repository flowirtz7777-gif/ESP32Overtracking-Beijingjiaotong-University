# CFG 配置生成器

用于生成 `转弯全过程盲区分析软件` 可导入的策略 CFG。

启动方式：

```text
双击 启动CFG配置生成器.bat
```

也可以直接用浏览器打开：

```text
index.html
```

桌面模式下点击 `保存 CFG` 会写入：

```text
C:\Users\Admin\Desktop\小挑资料\转弯全过程盲区分析软件\CFG配置
```

生成的 JSON 可以在盲区分析软件中通过 `导入策略 CFG / JSON` 使用。
