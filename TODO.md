# TODO

跟踪所有"想做但暂时不做"的工作。每项都标了**优先级**、**预计工作分支**与**接入条件**。

---

## ⏳ 待办

### 1. 逐行 CSV 回放动画 (`run_phase1_replay.m`)

- **优先级**: 中
- **目标分支**: `dev/phase1-replay-animation`
- **状态**: 待开始
- **目的**: 比当前 `run_phase1_demo` 更贴近硬件实际工作流，可演示 / 进论文

**功能要求**:
- 逐行读 CSV（每行 = 一次传感器读数，对齐 ESP32 50Hz 节拍）
- 每一行都跑完整 `predict_swept × 3` + `point_in_poly`
- 生成动画：
  - 当前车体位置（A/B/H/T 四点轮廓）
  - 当前帧的三层扫掠多边形（颜色按报警等级）
  - 雷达目标的实时风险等级（颜色、大小动态变化）
- 输出 `.mp4` 或 `.gif` 到 `Matlab/runs/<时间戳>__<csv名>/animation.mp4`
- 输出事件日志 `events.txt`：哪个时刻、哪个目标、升到了什么等级

**接入条件**: Phase 1 静态版 demo (run_phase1_demo) 通过验收门控之后再做。

**实现思路要点**:
- 用 `VideoWriter` 写 mp4，30 fps
- 报警状态机要带滞回（参考 CLAUDE.md 4.4 节）
- 先逐帧画图保证逻辑对，再考虑性能优化

---

## ✅ 已完成

（暂无；项目早期阶段所有事都直接做完了，没积累 done 列表的必要）

---

## 📌 维护约定

- 新增条目：放到「⏳ 待办」末尾，按"标题 + 优先级 + 目标分支 + 状态 + 详细描述"模板写
- 完成条目：从「⏳ 待办」移到「✅ 已完成」，附上完成日期和 commit hash
- 取消条目：直接删除，并在 commit message 里说明取消原因
