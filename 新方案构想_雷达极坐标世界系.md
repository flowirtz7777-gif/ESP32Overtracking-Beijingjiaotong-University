# 新方案构想：转向灯触发 + 锚点世界系 + 雷达极坐标判内

> 本文档记录对原有危险区计算架构的改进思路，由对话讨论整理而来。
> 对应分支：`dev/radar-polar-world-frame`

---

## 背景与动机

原方案将参考系固定在随车运动的车体坐标系（原点为牵引车后轴中心 B，x 指向车头）。
这带来两个工程问题：

1. **科里奥利效应**：车转弯时车体系在旋转，静止行人在车体系里会产生虚假切向速度，影响 TTC 计算精度。
2. **坐标变换开销**：雷达每帧输出的目标坐标需要逐目标转换到车体系，才能与危险区多边形比对。

新方案针对这两个问题提出改进。

---

## 核心思路

### 1. 转向灯触发激活

系统不持续运行，而是在**司机打开右转向灯时激活**。

- 激活时刻记为 `t = 0`
- 激活瞬间对传感器做一次校零（α、φ 零位确认）
- 系统在转向灯关闭或车辆恢复直行后复位

**优点**：
- 避免直行时的无效计算和误报
- 给系统一个明确的工作起点
- 符合驾驶员操作习惯，转向灯本身就是"即将转弯"的意图信号

**硬件接入**：转向灯信号接入 ESP32 一个 GPIO，检测高/低电平跳变。

---

### 2. 锚点局部世界坐标系

激活时刻，建立一个**局部惯性坐标系**并锁定：

```
原点：激活时刻车身位置（牵引车后轴中心 B 的地面投影）
Y 轴：激活时刻车身前进方向
X 轴：Y 轴正交方向（指向右侧）
```

此后该坐标系**不随车运动**，固定在地面上。

车辆自身位置通过对 `(v, θ)` 积分持续追踪：

```
xB_world += vB · cos(θ_world) · dt
yB_world += vB · sin(θ_world) · dt
θ_world  += ω1 · dt
```

**优点**：
- 坐标系为惯性系，行人对地速度即为其在此系中的速度，无科里奥利修正需求
- 静止行人坐标不随车转动而漂移
- TTC 计算更直观：目标在惯性系中的运动轨迹即为其真实轨迹

**需注意**：
- 无 GPS 情况下，车辆位置积分存在随时间累积的漂移误差
- 低速短时场景（5~15 km/h，单次转弯 < 10s）下漂移量可接受
- 如后续加装 GPS/IMU，可用于修正积分漂移

---

### 3. 危险区边界以雷达极坐标表达

扫掠危险区在世界坐标系内计算完成后，将边界离散点转换到**以雷达为原点的极坐标系**：

```
步骤一：世界系 → 雷达直角坐标系
  x_r = (x_world - x_radar) · cos(θ_radar) + (y_world - y_radar) · sin(θ_radar)
  y_r = -(x_world - x_radar) · sin(θ_radar) + (y_world - y_radar) · cos(θ_radar)

步骤二：雷达直角坐标系 → 极坐标
  r_i = sqrt(x_r² + y_r²)
  θ_i = atan2(y_r, x_r)
```

其中 `(x_radar, y_radar, θ_radar)` 为雷达安装偏置，通过标定获得，存入 `config.h`。

**此转换每次预测后执行一次（50 Hz），不随目标数量增加而重复。**

---

### 4. 判断逻辑在雷达极坐标系内完成

雷达直接输出目标的 `(r, θ)`，与危险区边界的离散 `(r_i, θ_i)` 点在同一坐标系内做 `point_in_poly` 判断。

```
对每个雷达目标 p = (r_target, θ_target)：
  将极坐标转回直角坐标：
    x = r · cos(θ)
    y = r · sin(θ)
  执行 point_in_poly(x, y, boundary_points_radar_frame)
  → 输出：在 PolyI / PolyA / PolyW 内，或不在
```

**注意**：不能简化为纯距离/角度大小比较。危险区是不规则多边形（非扇形），必须使用完整的 `point_in_poly` 算法（射线法或叉积法）。

---

## 与原方案对比

| 维度 | 原方案 | 新方案 |
|---|---|---|
| 参考系 | 滚动车体系（随车运动） | 激活时刻锁定的局部惯性系 |
| 触发条件 | 持续运行 | 右转向灯激活 |
| 目标坐标处理 | 每帧每目标做坐标变换 | 雷达数据零处理直接用 |
| 危险区表达 | 车体系直角坐标多边形 | 雷达极坐标离散边界点 |
| 科里奥利修正 | 工程近似忽略 | 惯性系内自然消除 |
| 积分漂移 | 无（每帧重置） | 有（随时间累积，短时可接受） |

---

## 待解决的工程问题

1. **雷达安装偏置标定**
   - 需要测量雷达相对于车体原点 B 的安装位置 `(dx, dy)` 和朝向角 `dθ`
   - 标定结果写入 `config.h` 作为 `static constexpr`

2. **转向灯信号接入**
   - 确定转向灯信号电平（12V 车载 → 需降压或光耦隔离到 3.3V）
   - ESP32 GPIO 检测上升沿触发激活

3. **积分漂移评估**
   - 在 MATLAB 仿真中量化：典型右转工况（10 km/h，转弯半径 10m，持续 8s）下的位置漂移量
   - 评估是否影响 PolyW / PolyA / PolyI 的判断准确性

4. **point_in_poly 算法确认**
   - 极坐标转直角后，使用射线法（ray casting）或叉积法
   - 与原车体系方案的判断结果做交叉验证

---

## 实施建议

此方案适合作为 **Phase 2+** 的改进方向，在原方案（车体系）完成 Phase 1 验收门控后再切换。

Phase 1 的 MATLAB 仿真框架可以直接复用，只需：
- 在 `kinematics_step.m` 外层加一个世界系积分包装
- 在 `predict_swept.m` 输出后加一个坐标转换函数 `poly_to_radar_polar.m`

---

*文档创建于 2026-05-29，基于与 Kiro 的架构讨论整理。*
